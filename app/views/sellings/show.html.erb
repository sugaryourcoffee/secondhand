<% provide(:title, t('.title')) %>

<h1><%= t('.heading', number: @selling.id) %></h1>
<h2><%= @event.title if @event %></h2>

<%= render 'show_selling_status' %>

<%= link_to t('.selling_overview'), sellings_path %>

<%= render 'items' %>
