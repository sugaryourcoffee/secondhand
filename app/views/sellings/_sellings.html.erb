<% if @sellings %>
  <table class="table table-hover" >
    <thead>
      <tr>
        <th><%= t('.selling_number') %></th>
        <th><%= t('.created') %></th>
        <th><%= t('.updated') %></th>
        <th><%= t('.revenue') %></th><th/><th/><th/>  
      </tr>
    </thead>
    <tbody>
      <% @sellings.each do |selling| %>
        <tr>
          <td><%= selling.id %></td>
          <td><%= selling.created_at %></td>
          <td><%= selling.updated_at %></td>
          <td><%= selling.total %></td>
          <td><%= link_to t('.show'), selling_path(selling) %></td>
          <td><%= link_to t('.delete'), 
                  selling, 
                  method: :delete, 
                  remote: true,
                  data: { confirm: t('.confirm_delete') } %>
          </td>
          <td><%= link_to t('.print') %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <%= t('.no_sellings') %>
<% end %>
