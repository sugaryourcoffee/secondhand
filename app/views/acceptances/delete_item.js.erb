<% if @item.errors.any? %>
  <%= add_gritter item_errors(@item), title: t('.error'), image: :error %>
<% end %>

$("#items-table").html('<%= escape_javascript(render partial: 'show_item', 
                                                     locals: { edit: false }) %>')

